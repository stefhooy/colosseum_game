"""
camera.py

World-to-screen coordinate transform. follow() centers the camera on a
target but clamps it so it never scrolls past the world bounds.

Placeholder for now — ported from the reference project (Tower of IE) in
Step 3, then extended with a zoomed-out virtual-resolution mode in Step 9.
"""
